package: _global_
defaults:
  - trainer: bc
  - dataset: minari
  - model: mlp_gauss
  - loss: bc_gaussian_nll
  - optimizer: adamw
  - evaluator: default_eval
  - _self_

exp:
  name: "fetch_bc"

dataset:
  path: "data/fetchreach_v2_expert_bc_v1"
  batch_size: 128
  num_workers: 4

trainer:
  max_steps: 30000
  eval_every: 5000
  save_every: 15000
